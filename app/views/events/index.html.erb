<h2 align="center">The Ezer Swing Five Is</h2>
<ul align="center">
<% Musician.where(:member => true).each do |musician| %>
		<li>
				<%= render :partial => "name_with_url", :locals => {:name => musician.name, :url => musician.url } %>
				<%= musician.instrument %>
		</li>
<% end %>
</ul>

<h2 align="center">Coming Up</h2>
<table align="center">
  <tr>
    <th>When?</th>
		<th>What?</th>
    <th>Where?</th>
    <th>Who?</th>
  </tr>

<%= render :partial => "events_list", :locals => {:events => @future_events} %>
</table>

<h2 align="center">Past Gigs</h2>
<table align="center">
  <tr>
    <th>When?</th>
		<th>What?</th>
    <th>Where?</th>
    <th>Who?</th>
  </tr>

<%= render :partial => "events_list", :locals => {:events => @past_events} %>
</table>
<br />

<% 	if current_user.try(:admin?) %>
	<%= link_to 'New Event', new_event_path %>
<% end %>